# 手机唤醒电脑（WOL 网络唤醒）设置清单

> 目标：电脑睡眠/关机(休眠)时，用手机 App 发一个“魔法包”把它唤醒。
> 大白话：电脑的网卡要“一直竖着耳朵听”，并且要能识别手机的“敲门声”，这需要 4 个地方依次打开。**每一步做完再进下一步。**

## 第 1 步：主板 BIOS（进 BIOS 打开唤醒开关）
1. 开机按 `Del`（或 `F2`，品牌不同，开机画面有提示）进 BIOS。
2. 找 **电源管理 / Power Management / ACPI** 一类菜单。
3. 打开这些项（不同主板叫法不同，找到类似的即可）：
   - **Wake on LAN / 网络唤醒 / 从 PCI-E 设备唤醒** → Enabled（开）
   - **ErP / EUP 节能** → 若开启会切断待机供电，**请设为 Disabled（关）**（这步很关键，很多唤醒失败都因为它）
4. 保存退出（一般按 `F10` → Yes）。

## 第 2 步：Windows 网卡设置（必做）
1. 右键“开始”→ 设备管理器 → 展开 **网络适配器**。
2. 找到你上网用的那张网卡（无线网卡一般叫 **WLAN / Intel Wireless**；插网线的叫 **Ethernet / Realtek / Intel I225** 之类）→ 双击打开属性。
3. **电源管理** 页：
   - ☑ 勾选 **允许计算机关闭此设备以节约电源**（保留勾选没关系）
   - ☑ 勾选 **允许此设备唤醒计算机**
4. **高级** 页，找到并设为 Enabled（开）：
   - **Wake on Magic Packet / 魔术包唤醒**
   - **关机 网络唤醒 (Wake on Shutdown)**
   - 无线网卡如果有 **Wake on Wireless LAN / WoWLAN**，也开。
5. 确定保存。

> 提醒：睡眠状态基本都能唤醒；**完全关机（断电）只有插网线 + 主板支持才可能唤醒**，无线 WiFi 一般只能唤醒“睡眠/休眠”，不能唤醒“断电关机”。

## 第 3 步：Windows 系统（别让“快速启动”捣乱）
1. 控制面板 → 电源选项 → 选择电源按钮的功能 → **更改当前不可用的设置**。
2. 取消勾选 **启用快速启动**（它会干扰断电唤醒），保存。
3. 可选：把“睡眠”设为合适时间，或按需手动睡眠来测试。

## 第 4 步：路由器（局域网内唤醒通常不用动；跨网段才需要）
- 手机和电脑在**同一个 WiFi/同一个路由器**下 → 通常**不用**动路由器，直接进第 5 步。
- 手机想用“移动网络/外部网络”远程唤醒 → 才需要路由器端口转发/UDP 广播（高级玩法，先用局域网打通再考虑）。

## 第 5 步：手机装 App 并填入信息（关键字段）
任选一个免费 App（Android：Wake On Lan；iPhone：Mocha WOL 等）。填三样：
- **MAC 地址**：在 DSH Studio“手机接力”面板里能看到（每张网卡一行，格式如 `AA-BB-CC-DD-EE-FF`）。填“上网那张卡”对应的 MAC。
- **IP 地址**：电脑当前局域网 IP（如 `192.168.31.227`；面板简单地址里也能看到）。
- **子网掩码**：家用一般 `255.255.255.0`；广播地址自动算（多为 `192.168.31.255`）。

## 测试
1. 电脑**睡眠**（不是关窗口，是让系统睡眠）。
2. 手机 App 里点“唤醒/开机”。
3. 正常的话 3~10 秒电脑亮屏。
> 失败排查顺序：先确认第 2 步两张“唤醒”勾选都在 → 再查 BIOS 里 ErP 关没关 → 再确认填的 MAC 是“正在上网那张卡”。
